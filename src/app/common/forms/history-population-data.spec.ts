import { HistoryPopulationData } from './history-population-data';

describe('HistoryPopulationData', () => {
  it('should create an instance', () => {
    expect(new HistoryPopulationData()).toBeTruthy();
  });
});
